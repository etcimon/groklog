{
  "grok": {
    "apiKey": "",
    "baseUrl": "https://api.x.ai/v1",
    "promptTemplate": "Research the latest updates on \"{subject}\" since {lastDate}. Aggregate from multiple sources, summarize and re-elaborate as industry-focused information for experts in 2 paragraphs of details. Return a JSON array of objects where source_urls is a comma-separated list of urls: [{datetime: ISO, importance: 1-10, summary: str, details: str, source_urls?: str}].",
    "systemPrompt": "Given the results of research and information processing, respond using structured JSON as requested"
  },
  "db": {
    "path": "./groklog.db"
  }
}